import React from 'react';

import { Spinner } from '../..';

export default {
  title: 'Spinner',
};

export const Base = () => (
  <div className="flex vertical justify-center">
    <Spinner />
  </div>
);
